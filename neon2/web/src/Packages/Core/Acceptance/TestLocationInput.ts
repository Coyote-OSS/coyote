import {LocationInput, LocationListener} from "../Application/LocationInput";

export class TestLocationInput implements LocationInput {
  mount(input: HTMLInputElement, listener: LocationListener): void {
  }
}
